# Aime 与飞书任务存储设计

## 背景

Aime 的核心不应该是另一个聊天助手，而是桌面上的任务感知体。它通过桌面输入、截图上下文、用户勾选和主动建议，把分散的工作意图收束成可执行任务。

飞书 Base 适合作为任务存储地，承载任务记录、状态同步和后续协作。桌面端 Aime 负责捕捉、判断、提醒和执行，保留轻量存在感。

## 产品定位

- 飞书 Base 是任务仓库，不是主要任务入口。
- 桌面 Aime 是任务捕捉与执行层，负责新增任务、自动识别、下一步建议和完成反馈。
- Aime 小狗是用户感知层，提供低打扰状态反馈，不做重聊天入口。

## 任务来源

任务主要来自桌面端：

- 用户把会议纪要、聊天记录等材料丢给 Aime 助手，Aime 自动总结出任务，写入本地展示；用户也可以直接用自然语言告诉 Aime 要新增什么任务。
- 用户在桌面端直接新增、删除或调整任务。
- 桌面端基于截图、当前窗口、输入行为或手动捕捉自动识别潜在任务。
- Aime 根据上下文主动提出任务建议，用户接受后才落库。
- 未来可补充从飞书文档、会议、聊天中导入，但不作为第一阶段主入口。

## 数据边界

飞书 Base 存储跨端和可协作字段：

- 标题
- 状态
- 截止时间
- 来源类型
- 来源链接
- 项目或类别
- 负责人

本地存储体验字段：

- 隐藏状态
- 置顶和展示优先级
- 狗粮奖励状态
- Aime 小狗状态
- 意图历史
- 主动建议反馈

## 同步行为

- 新增任务：桌面端创建后写入飞书 Base，并同步回本地快照。
- 勾选完成：写回飞书状态为已完成，本地把任务沉底展示。
- 取消勾选：写回飞书状态为未完成，本地恢复到待办队列。
- 隐藏任务：只保存在本地，不写回飞书，避免影响团队任务源。
- 自动建议：只有用户接受后才创建飞书任务。

## Aime 助手存在形态

Aime 需要作为一个存在，但不是聊天机器人式助手。

它应该表现为一个轻量任务生命体：

- 有状态：空闲、发现任务、等待确认、完成奖励、逾期关注。
- 有动作：提醒下一件事、展开待办、接收勾选、沉底已完成任务。
- 有边界：不主动发送飞书消息，不自动创建任务，不替用户做不可逆操作。
- 有温度：用小狗和狗粮反馈降低压力，但不遮挡用户工作。

## 第一阶段落地范围

第一阶段只做任务存储闭环：

- Aime 助手接收会议纪要、聊天记录或用户自然语言输入，生成待确认任务，并写入本地展示。
- 桌面端新增任务写入本地展示和飞书 Base。
- 桌面端删除任务时，优先本地隐藏；需要真正删除飞书记录时必须二次确认。
- 首次启动和手动同步从飞书 Base 拉取任务。
- 勾选完成和取消勾选写回飞书状态。
- 本地继续维护置顶、隐藏、奖励和意图状态。
- 页面上显示同步状态，让用户知道当前是否已连接飞书。

## 非目标

- 不把 Aime 做成完整聊天助手。
- 不自动扫描或全量记录用户行为。
- 不在用户未确认时创建飞书任务。
- 不把狗粮、隐藏、意图历史等私人体验状态写入飞书。

## 验收标准

- 用户在桌面端新增任务后，飞书 Base 中能看到对应记录。
- 用户把会议纪要或聊天记录交给 Aime 后，本地能展示提炼出的待确认任务。
- 用户删除任务时，本地列表不再展示；如果涉及飞书记录删除，需要明确确认。
- 用户在桌面端勾选完成后，飞书 Base 状态变为已完成。
- 用户取消勾选后，飞书 Base 状态恢复为未完成。
- 飞书 Base 任务重新同步后，本地仍保留隐藏、置顶和狗粮状态。
- 没有飞书授权或字段配置错误时，用户能看到清晰的同步异常状态，本地体验不崩溃。
